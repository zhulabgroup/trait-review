---
title: "workflow"
author: "Yiluan Song"
date: "2023-10-02"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(traitlitreview)
```

```{r, eval = F}
df_bib <- read_bib(path = "alldata/input/bib/20230918", num_core = 4)
df_bib_code <- get_bib_code(df_bib = df_bib)
```

```{r, eval = F}
df_bib_phrase <- get_all_keyphrase(df_bib, num_core = 30)
df_phrase_stem <- get_keyphrase_stem(df_bib_phrase = df_bib_phrase)
df_phrase_label <- read_keyphrase_label()
```

```{r, eval = F}
ls_df_net <- make_network(df_bib_phrase, df_phrase_stem, df_phrase_label, df_bib_code)
```

```{r}
plot_text_network(ls_df_net$area_trait, min_count = 50, save = T, filename = "area_trait")
# plot_text_network(ls_df_net$trait_all , min_count = 100, save = T)
plot_text_network(ls_df_net$trait_gc, min_count = 800, save = T, filename = "trait_gc")
```
